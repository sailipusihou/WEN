# 东方好物集市 - 项目说明书

## 1. 项目概述

**项目名称：** Oriental Treasures Market（东方好物集市）
**技术栈：** Next.js 15 + TypeScript + SQLite + Tailwind CSS
**项目类型：** 跨境电商独立站（东方工艺品/文创产品）

### 1.1 核心功能模块

| 模块 | 功能说明 |
|------|----------|
| 商品管理 | 商品增删改查、分类管理、精选商品、库存管理 |
| 订单管理 | 订单创建、状态流转、物流追踪、员工分配 |
| 用户系统 | 注册登录、个人中心、地址管理、收藏夹 |
| 管理后台 | 仪表盘、商品/订单/用户管理、系统设置 |
| 内容系统 | 评价管理、留言消息、邮件订阅 |
| SEO优化 | 动态元数据、Open Graph、Twitter Cards、结构化数据 |
| 数据存储 | 支持 JSON 文件 和 SQLite 双后端切换 |

---

## 2. 目录结构

```
project-root/
├── app/                          # Next.js App Router
│   ├── page.tsx                  # 首页
│   ├── layout.tsx                # 全局布局
│   ├── products/                 # 商品相关页面
│   │   ├── page.tsx              # 商品列表页
│   │   └── [id]/page.tsx         # 商品详情页
│   ├── category/[slug]/page.tsx  # 分类页
│   ├── search/page.tsx           # 搜索页
│   ├── admin/                    # 管理后台
│   │   ├── page.tsx              # 仪表盘
│   │   ├── products/             # 商品管理
│   │   ├── orders/               # 订单管理
│   │   ├── categories/           # 分类管理
│   │   ├── users/                # 用户管理
│   │   └── settings/             # 系统设置
│   └── api/                      # API 路由
│       ├── products/             # 商品 API
│       ├── orders/               # 订单 API
│       ├── categories/           # 分类 API
│       ├── users/                # 用户 API
│       ├── auth/                 # 认证 API
│       ├── reviews/              # 评价 API
│       ├── messages/             # 留言 API
│       ├── settings/             # 设置 API
│       ├── wishlist/             # 收藏夹 API
│       └── newsletter/           # 邮件订阅 API
├── components/                   # React 组件
│   ├── layout/                   # 布局组件（Header、Footer、HomeClient）
│   ├── product/                  # 商品组件（ProductCard、ProductDetailClient）
│   ├── ui/                       # UI 组件（OptimizedImage、CategoryCard）
│   └── admin/                    # 管理后台组件
├── lib/                          # 工具库
│   ├── repository.ts             # 数据访问层统一入口
│   ├── db/                       # 数据层
│   │   ├── repository-sqlite.ts  # SQLite 版本 Repository
│   │   └── ...                   # 旧版 JSON 模块
│   ├── orders.ts                 # 订单相关（JSON 后端）
│   ├── users.ts                  # 用户相关（JSON 后端）
│   ├── categories.ts             # 分类相关（JSON 后端）
│   ├── settings.ts               # 系统设置
│   ├── auth.ts                   # 认证与权限
│   ├── permissions.ts            # 权限配置
│   ├── cache.ts                  # 缓存工具
│   ├── email.ts                  # 邮件服务
│   └── validation.ts             # 输入验证
├── data/                         # JSON 数据文件（默认后端）
├── scripts/                      # 脚本工具
│   └── migrate-json-to-sqlite.ts # JSON → SQLite 迁移脚本
├── public/                       # 静态资源
└── site.db                       # SQLite 数据库文件（使用 SQLite 后端时生成）
```

---

## 3. 数据访问层（Repository 模式）

### 3.1 设计理念

项目采用 **Repository 模式** 抽象数据访问层，支持两种后端存储：

- **JSON 文件后端**（默认）：数据存储在 `data/` 目录下的 JSON 文件中，适合开发和小规模使用
- **SQLite 后端**：数据存储在 `site.db` 文件中，适合生产环境和数据量较大的场景

通过环境变量 `DATABASE_BACKEND` 切换，无需修改业务代码。

### 3.2 Repository 接口

```typescript
interface DataRepository {
  products:    { list, listActive, getById, getByCategory, add, update, delete }
  reviews:     { list, getByProduct, add, update }
  orders:      { list, getById, add, update }
  users:       { list, getById, getByEmail, getByToken, add, update }
  categories:  { list, getBySlug, add, update, delete }
  messages:    { list, getById, add, update }
  settings:    { get, update }
  staff:       { list, getById, getByEmail, add, update, delete }
  newsletter:  { list, getByEmail, add, delete }
  workLogs:    { list, add }
}
```

### 3.3 使用方式

```typescript
import { getRepository } from '@/lib/repository'

const repo = getRepository()

// 获取商品列表
const products = repo.products.list()

// 获取单个商品
const product = repo.products.getById('product-id')

// 更新商品
repo.products.update('product-id', { price: 99.99 })
```

---

## 4. 数据库说明

### 4.1 两种存储后端对比

| 特性 | JSON 文件 | SQLite |
|------|-----------|--------|
| 配置难度 | 零配置，开箱即用 | 需要运行迁移脚本 |
| 数据文件位置 | `data/*.json` | 项目根目录 `site.db` |
| 并发性能 | 较差（文件锁） | 较好（事务支持） |
| 查询能力 | 全量遍历 | SQL 查询，支持索引 |
| 数据一致性 | 较差 | 较好（ACID） |
| 适合场景 | 开发、小规模 | 生产、中大规模 |

### 4.2 切换到 SQLite

```bash
# 1. 运行迁移脚本（将 JSON 数据导入 SQLite）
npx tsx scripts/migrate-json-to-sqlite.ts

# 2. 设置环境变量（.env.local）
DATABASE_BACKEND=sqlite

# 3. 重启开发服务器
npm run dev
```

### 4.3 回退到 JSON

去掉 `.env.local` 中的 `DATABASE_BACKEND=sqlite` 或设置为 `json`，重启服务即可。

---

## 5. 认证与权限系统

### 5.1 用户角色

| 角色 | 说明 |
|------|------|
| `super_admin` | 超级管理员，拥有所有权限 |
| `admin` | 管理员，大部分管理权限 |
| `staff` | 员工，订单处理等基础权限 |
| `customer` | 普通用户（前端注册用户） |

### 5.2 权限配置

权限定义在 `lib/permissions.ts` 中，采用 **角色 → 权限** 的映射关系。

### 5.3 管理员密码安全

- 密码使用 PBKDF2 哈希存储（100,000 次迭代，SHA-512）
- 首次登录时自动从明文密码迁移为哈希存储
- 员工密码同样使用安全哈希

---

## 6. 关键 API 列表

### 6.1 商品 API

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/products` | 商品列表（支持分页、搜索、筛选） | 公开 |
| GET | `/api/products/[id]` | 商品详情 | 公开 |
| POST | `/api/products` | 新增商品 | 管理员 |
| PUT | `/api/products/[id]` | 更新商品 | 管理员 |
| DELETE | `/api/products/[id]` | 删除商品 | 管理员 |

### 6.2 订单 API

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/orders` | 订单列表（支持分页、搜索、状态筛选） | 管理员 |
| GET | `/api/orders?email=xxx` | 用户的订单 | 对应用户 |
| POST | `/api/orders` | 创建订单 | 公开 |
| PUT | `/api/orders` | 更新订单状态/物流/分配 | 订单处理权限 |

### 6.3 认证 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth` | 管理员登录 |
| POST | `/api/auth/logout` | 管理员登出 |
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/user-login` | 用户登录 |
| POST | `/api/auth/user-logout` | 用户登出 |
| GET | `/api/auth/user` | 获取当前用户信息 |
| PUT | `/api/auth/user` | 更新用户信息 |

---

## 7. 前端页面说明

### 7.1 公开页面

- **首页** (`/`)：Hero 区域、分类导航、精选商品、品牌故事、邮件订阅
- **商品列表** (`/products`)：所有商品，支持筛选和排序
- **商品详情** (`/products/[id]`)：商品图片库、详情、评价、加入购物车
- **分类页** (`/category/[slug]`)：分类下的商品列表
- **搜索页** (`/search`)：关键词搜索、搜索历史、热门搜索
- **购物车**：侧边抽屉式购物车
- **用户中心**：账户信息、地址管理、订单查询、收藏夹

### 7.2 管理后台页面

- **仪表盘** (`/admin`)：数据概览、销售统计、订单趋势
- **商品管理** (`/admin/products`)：商品列表、新增、编辑、删除
- **订单管理** (`/admin/orders`)：订单列表、状态处理、物流追踪、员工分配
- **分类管理** (`/admin/categories`)：分类增删改、图标上传
- **用户管理** (`/admin/users`)：用户列表、用户信息编辑
- **系统设置** (`/admin/settings`)：站点信息、支付物流、员工管理、自动分配设置

---

## 8. 开发与部署

### 8.1 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

### 8.2 环境变量

在 `.env.local` 中配置：

```env
# 数据库后端：json 或 sqlite
DATABASE_BACKEND=json

# SMTP 邮件配置（可选）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email
SMTP_PASS=your-password
```

### 8.3 部署建议

1. **生产环境建议使用 SQLite 后端**，性能和数据一致性更好
2. **定期备份数据库**：`site.db` 文件就是全部数据，直接复制即可备份
3. **配置反向代理**：使用 Nginx 等反向代理，启用 HTTPS
4. **环境变量**：确保生产环境设置正确的环境变量

---

## 9. 常见问题

### Q: 管理后台登录密码是什么？
A: 默认管理员账号在 `lib/settings.ts` 的 `DEFAULTS` 中，首次登录后会自动迁移为安全哈希存储。建议登录后立即修改。

### Q: 如何修改站点名称、Logo 等信息？
A: 登录管理后台 → 系统设置 → 基本设置，可以修改所有站点信息。

### Q: 商品图片存在哪里？
A: 目前使用的是 Unsplash 远程图片作为演示。实际使用时可以上传到 `/public` 目录或使用对象存储服务。

### Q: 如何添加新的管理员/员工？
A: 登录管理后台 → 系统设置 → 员工管理，可以添加、编辑、删除员工账号并分配角色。

---

## 10. 技术栈细节

- **框架：** Next.js 15 App Router
- **语言：** TypeScript
- **样式：** Tailwind CSS
- **动画：** Framer Motion
- **图标：** Lucide React
- **数据库：** SQLite（better-sqlite3）
- **认证：** 自研 Cookie-based 认证
- **邮件：** Nodemailer（可选）
