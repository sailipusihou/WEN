# 数据库操作指南

## 目录
1. [数据库存放位置](#1-数据库存放位置)
2. [如何查看数据库内容](#2-如何查看数据库内容)
3. [常用数据库操作](#3-常用数据库操作)
4. [表结构说明](#4-表结构说明)
5. [数据迁移](#5-数据迁移)
6. [备份与恢复](#6-备份与恢复)
7. [常见问题](#7-常见问题)

---

## 1. 数据库存放位置

### 1.1 SQLite 数据库文件

当使用 SQLite 后端时，数据库文件位于：

```
项目根目录/
└── site.db    ← 这就是数据库文件
```

**通俗解释：** 就像一个 Excel 文件，所有数据（商品、订单、用户等）都存在这一个文件里。你可以把它复制、备份、带走，非常方便。

### 1.2 JSON 文件（默认后端）

如果使用的是 JSON 后端（默认），数据在 `data/` 目录下：

```
项目根目录/
└── data/
    ├── products.json       # 商品数据
    ├── orders.json         # 订单数据
    ├── users.json          # 用户数据
    ├── categories.json     # 分类数据
    ├── reviews.json        # 评价数据
    ├── messages.json       # 留言数据
    ├── newsletter.json     # 邮件订阅数据
    └── work-logs.json      # 操作日志
```

### 1.3 怎么知道当前用的是哪种？

查看项目根目录下的 `.env.local` 文件：

- 如果有 `DATABASE_BACKEND=sqlite` → 用的是 SQLite（`site.db`）
- 如果没有这一行，或者是 `DATABASE_BACKEND=json` → 用的是 JSON 文件（`data/` 目录）

---

## 2. 如何查看数据库内容

### 2.1 方法一：使用图形化工具（推荐，最直观）

推荐使用 **DB Browser for SQLite**，它是免费的、中文界面的，像 Excel 一样操作。

**下载地址：** https://sqlitebrowser.org/dl/

**使用步骤：**

1. 下载并安装 DB Browser for SQLite
2. 打开软件，点击「打开数据库」
3. 选择项目根目录下的 `site.db` 文件
4. 点击「浏览数据」标签页
5. 在下拉菜单中选择要查看的表（比如 products 商品表）
6. 就可以看到所有数据了，还可以直接修改、添加、删除

### 2.2 方法二：使用命令行

如果你熟悉命令行，可以用 SQLite 自带的工具：

```bash
# 进入项目目录
cd 项目路径

# 打开数据库
sqlite3 site.db

# 查看所有表
.tables

# 查看商品表结构
.schema products

# 查询所有商品
SELECT * FROM products;

# 退出
.quit
```

### 2.3 方法三：通过管理后台查看

大部分常用数据都可以通过管理后台界面查看和修改：

- 商品管理 → 查看/编辑商品
- 订单管理 → 查看/处理订单
- 用户管理 → 查看用户信息
- 分类管理 → 管理商品分类
- 系统设置 → 修改站点配置

---

## 3. 常用数据库操作

### 3.1 查看所有商品

```sql
SELECT id, name, nameEn, price, category, active 
FROM products 
ORDER BY createdAt DESC;
```

### 3.2 查看所有订单

```sql
SELECT id, status, total, userEmail, createdAt 
FROM orders 
ORDER BY createdAt DESC 
LIMIT 20;
```

### 3.3 查看某个用户的订单

```sql
SELECT id, status, total, createdAt 
FROM orders 
WHERE userEmail = 'user@example.com' 
ORDER BY createdAt DESC;
```

### 3.4 修改商品价格

```sql
UPDATE products 
SET price = 99.99, updatedAt = datetime('now')
WHERE id = 'product-id';
```

### 3.5 删除某个商品

```sql
DELETE FROM products WHERE id = 'product-id';
```

### 3.6 统计订单数量和销售额

```sql
SELECT 
  COUNT(*) as 订单总数,
  SUM(CASE WHEN status != 'cancelled' THEN total ELSE 0 END) as 总销售额
FROM orders;
```

### 3.7 按状态统计订单

```sql
SELECT status, COUNT(*) as 数量
FROM orders
GROUP BY status;
```

---

## 4. 表结构说明

### 4.1 商品表（products）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | TEXT | 商品ID（主键） |
| name | TEXT | 商品名称（中文） |
| nameEn | TEXT | 商品名称（英文） |
| slug | TEXT | URL 友好的标识 |
| category | TEXT | 分类 slug |
| price | REAL | 价格 |
| originalPrice | REAL | 原价（划线价） |
| image | TEXT | 主图 URL |
| images | TEXT | 图片列表（JSON数组） |
| detailImages | TEXT | 详情图（JSON数组） |
| description | TEXT | 商品描述（中文） |
| descriptionEn | TEXT | 商品描述（英文） |
| subtitle | TEXT | 副标题（中文） |
| subtitleEn | TEXT | 副标题（英文） |
| featured | INTEGER | 是否精选（1=是，0=否） |
| active | INTEGER | 是否上架（1=上架，0=下架） |
| stock | INTEGER | 库存数量 |
| rating | REAL | 评分 |
| reviewCount | INTEGER | 评价数 |
| material | TEXT | 材质 |
| craft | TEXT | 工艺（中文） |
| craftEn | TEXT | 工艺（英文） |
| origin | TEXT | 产地 |
| dimensions | TEXT | 尺寸 |
| weight | TEXT | 重量 |
| createdAt | TEXT | 创建时间 |
| updatedAt | TEXT | 更新时间 |

### 4.2 订单表（orders）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | TEXT | 订单ID（主键） |
| userId | TEXT | 用户ID |
| userEmail | TEXT | 用户邮箱 |
| customerName | TEXT | 客户姓名 |
| customerEmail | TEXT | 客户邮箱 |
| items | TEXT | 订单商品（JSON） |
| subtotal | REAL | 小计 |
| shippingCost | REAL | 运费 |
| total | REAL | 总价 |
| currency | TEXT | 货币 |
| status | TEXT | 订单状态 |
| statusHistory | TEXT | 状态历史（JSON） |
| notes | TEXT | 订单备注 |
| shipping | TEXT | 收货地址（JSON） |
| billing | TEXT | 账单地址（JSON） |
| tracking | TEXT | 物流信息（JSON） |
| trackingUrl | TEXT | 物流追踪链接 |
| estimatedDelivery | TEXT | 预计送达时间 |
| assignedTo | TEXT | 分配的员工ID |
| assignedToName | TEXT | 分配的员工姓名 |
| assignedToAvatar | TEXT | 分配的员工头像 |
| paymentMethod | TEXT | 支付方式 |
| paymentStatus | TEXT | 支付状态 |
| createdAt | TEXT | 创建时间 |
| updatedAt | TEXT | 更新时间 |

### 4.3 用户表（users）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | TEXT | 用户ID（主键） |
| email | TEXT | 邮箱（唯一） |
| passwordHash | TEXT | 密码哈希 |
| salt | TEXT | 密码盐值 |
| firstName | TEXT | 名 |
| lastName | TEXT | 姓 |
| phone | TEXT | 电话 |
| avatar | TEXT | 头像URL |
| dob | TEXT | 出生日期 |
| gender | TEXT | 性别 |
| bio | TEXT | 个人简介 |
| preferredCurrency | TEXT | 偏好货币 |
| role | TEXT | 角色（customer/admin等） |
| token | TEXT | 登录token |
| createdAt | TEXT | 创建时间 |
| updatedAt | TEXT | 更新时间 |

### 4.4 分类表（categories）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| slug | TEXT | 分类标识（主键） |
| name | TEXT | 分类名（中文） |
| nameEn | TEXT | 分类名（英文） |
| description | TEXT | 描述（中文） |
| descriptionEn | TEXT | 描述（英文） |
| image | TEXT | 分类图片 |
| icon | TEXT | 图标类型 |
| sortOrder | INTEGER | 排序 |
| active | INTEGER | 是否启用 |
| productCount | INTEGER | 商品数量 |
| createdAt | TEXT | 创建时间 |
| updatedAt | TEXT | 更新时间 |

### 4.5 其他表

- **reviews** - 商品评价表
- **user_addresses** - 用户地址表
- **wishlist** - 收藏夹表
- **messages** - 留言消息表
- **newsletter** - 邮件订阅表
- **work_logs** - 操作日志表
- **site_settings** - 系统设置表

---

## 5. 数据迁移

### 5.1 从 JSON 迁移到 SQLite

```bash
# 1. 确保项目依赖已安装
npm install

# 2. 运行迁移脚本
npx tsx scripts/migrate-json-to-sqlite.ts

# 3. 看到 "迁移完成！" 的提示就成功了
```

**迁移脚本会做什么？**
- 读取 `data/` 目录下的所有 JSON 文件
- 创建 SQLite 数据库表结构
- 把所有数据导入到 `site.db` 中
- 迁移过程中不会修改或删除原有的 JSON 文件

### 5.2 切换到 SQLite 后端

在项目根目录创建或编辑 `.env.local` 文件，添加：

```env
DATABASE_BACKEND=sqlite
```

然后重启开发服务器：

```bash
npm run dev
```

### 5.3 如何验证迁移成功？

1. 启动服务器后，访问管理后台
2. 看看商品、订单、用户等数据是否都在
3. 或者用 DB Browser 打开 `site.db` 查看数据

---

## 6. 备份与恢复

### 6.1 备份数据库（SQLite）

**最简单的方法：直接复制文件！**

```bash
# 备份（复制一份就好）
cp site.db site.db.backup

# 或者带日期的备份
cp site.db backup/site.db.$(date +%Y%m%d)
```

**建议备份频率：**
- 开发阶段：每周备份一次
- 生产环境：每天自动备份
- 重要操作前：手动备份一次

### 6.2 恢复数据库（SQLite）

```bash
# 停止服务器
# 用备份文件替换当前文件
cp site.db.backup site.db

# 重启服务器
```

### 6.3 备份 JSON 数据

直接复制整个 `data/` 目录即可：

```bash
cp -r data data.backup
```

---

## 7. 常见问题

### Q: 数据库文件可以用 Excel 打开吗？
A: 不能直接打开，但你可以：
1. 用 DB Browser for SQLite 打开
2. 导出为 CSV 格式（文件 → 导出 → 表为CSV）
3. 然后用 Excel 打开 CSV 文件

### Q: 误删了数据怎么办？
A: 
1. 先停止服务器，避免数据被覆盖
2. 看看有没有备份文件
3. 如果有备份，用备份恢复
4. 如果没有备份，数据很难恢复（SQLite 删除后很难找回）
5. **重要：操作前一定要备份！**

### Q: 数据库文件多大？
A: 
- 刚开始只有几十 KB
- 1000 个商品大概几百 KB
- 10000 个订单大概几 MB
- 非常小，不用担心空间问题

### Q: 多人同时操作会出问题吗？
A: 
- SQLite 支持并发读取，但写入时会锁库
- 小型网站（每天几千访问量）完全没问题
- 如果访问量很大，建议改用 MySQL/PostgreSQL

### Q: 怎么把数据库传到服务器？
A: 
1. 用 FTP 或其他方式把 `site.db` 文件上传到服务器项目根目录
2. 确保服务器上的 `.env.local` 配置了 `DATABASE_BACKEND=sqlite`
3. 重启 Node.js 服务

### Q: JSON 和 SQLite 能切换着用吗？
A: 可以，但是：
- 切换后端后，两边的数据是独立的
- 比如你在 JSON 里加了一个商品，切换到 SQLite 后看不到
- 建议确定后就固定用一种后端

---

## 8. 小技巧

### 8.1 快速统计数据

```sql
-- 商品总数
SELECT COUNT(*) FROM products;

-- 上架商品数
SELECT COUNT(*) FROM products WHERE active = 1;

-- 精选商品数
SELECT COUNT(*) FROM products WHERE featured = 1;

-- 注册用户数
SELECT COUNT(*) FROM users;

-- 今日订单数
SELECT COUNT(*) FROM orders WHERE DATE(createdAt) = DATE('now');
```

### 8.2 查找某个商品

```sql
-- 按名称搜索（模糊搜索）
SELECT * FROM products WHERE name LIKE '%茶具%' OR nameEn LIKE '%tea%';
```

### 8.3 修改管理员密码

如果忘记了管理员密码，可以用 SQL 直接重置（需要知道如何生成哈希，建议通过后台的「忘记密码」功能，或者直接在管理后台的员工管理中修改）。

---

**记住：操作数据库前一定要备份！** 就像改文件前先复制一份一样，安全第一！
